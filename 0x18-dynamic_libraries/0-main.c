#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * main - entry point, checks code
 *
 * Return: Always EXIT_SUCCESS.
 */
int main(void)
{
    printf("%ld\n", strlen("My Dyn Lib"));
    return (0);
}
